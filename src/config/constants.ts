export const ANIMATION_DELAY = 250
export const PROCESS_DELAY = ANIMATION_DELAY * 2

export enum COOKIE_NAMES {
  access_token = 'acc-tkn',
  refresh_token = 're_tkn',
  viewport = 'vp',
}

export enum VIEWPORT {
  mobile = 'mobile',
  tablet = 'tablet',
  desktop = 'desktop',
}

export enum QUERY_PARAM_KEYS {
  search_term = 'srch_trm',
}